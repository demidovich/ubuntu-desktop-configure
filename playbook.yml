# playbook.yml:
---
- name: "My ubuntu desktop playbook"
  hosts: 127.0.0.1
  connection: local
  become: true
  gather_facts: no
  tasks:

    - name: Update apt repo and cache
      apt:
        update_cache: yes
        force_apt_get: yes
        cache_valid_time: 3600

    - name: Install apt packages
      apt: 
        name:
          - net-tools
          - htop
          - lnav
          - nethogs
          - ncdu
          - pydf
          - git
          - git-flow
          - make
          - mc
          - alacarte
          - firefox
          - flameshot
          - vokoscreen
          - p7zip
          - ca-certificates
          - php
          - gnome-tweaks
          - synaptic
          - ubuntu-restricted-extras
        state: latest

    - name: Enabel firewall
      shell: |
        ufw enable
        ufw allow samba
